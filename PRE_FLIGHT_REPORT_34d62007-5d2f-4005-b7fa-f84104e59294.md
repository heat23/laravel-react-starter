Model: haiku

# Pre-Flight Quality Gate Report

**Session ID:** 34d62007-5d2f-4005-b7fa-f84104e59294

**Overall Status:** FAIL (blocking test failures detected)

---

## Gate Results Summary

| Gate | Status | Details |
|------|--------|---------|
| PHP Tests | FAIL | 2 failed, 2 risky, 6 skipped, 1395 passed |
| JS/TS Tests | FAIL | 6 failed, 1469 passed |
| Build | PASS | Production build successful |
| Lint (ESLint) | PASS | 97 warnings (no errors) |
| TypeScript | PASS | No type errors |
| PHPStan | PASS | No errors |
| Composer Audit | PASS | No vulnerabilities |
| npm Audit | PASS | No critical vulnerabilities |

---

## Failed Tests - PHP

### 1. `TrialNudgeNotificationTest::it renders email 2 — trial ending soon`
**File:** `tests/Unit/Notifications/TrialNudgeNotificationTest.php:24`

```
Expected: {{APP_NAME}}: Your trial ends in 4 days
To contain: days left
```

The test expects the notification subject to contain "days left" but the actual subject uses a different format "Your trial ends in X days".

### 2. `WelcomeSequenceNotificationTest::it renders email 3 — features list`
**File:** `tests/Unit/Notifications/WelcomeSequenceNotificationTest.php:53`

```
Expected: {{APP_NAME}}: Features you might not have found yet
To contain: What you can do with
```

The test expects the notification subject to contain "What you can do with" but the actual subject uses "Features you might not have found yet".

---

## Failed Tests - JavaScript/TypeScript

### File: `resources/js/Pages/Onboarding.test.tsx`

**6 failed tests:**
1. `navigates to step 2 on Next` — Unable to find element with text "Next"
2. `shows timezone selector in step 2` — Missing step 2 content
3. `shows theme options in step 2` — Missing step 2 content
4. `shows feature cards in step 3` — Unable to find "Dashboard" text
5. `can navigate back from step 2` — Unable to find "set your preferences" text
6. `shows onboarding complete screen` — Unable to find "you're ready to ship" text

These failures indicate the Onboarding component structure has changed but the test assertions were not updated to match the new HTML structure.

---

## Build Results

**Status:** PASS
**Duration:** 2.75s for client, 493ms for SSR

The production build completed successfully with warnings about dynamic vs static imports (expected for the smoke-test setup) but no breaking errors.

---

## Lint Results

**Status:** PASS (warnings only)
- Total: 97 warnings, 0 errors
- 76 warnings fixable with `--fix`
- Common issues: import ordering, unused variables, fast-refresh violations

These are non-blocking and can be fixed separately.

---

## TypeScript & Static Analysis

**Status:** PASS
- TypeScript: No type errors
- PHPStan: No errors

---

## Security

**Status:** PASS
- Composer audit: No vulnerabilities
- npm audit: No critical vulnerabilities

---

## Note on Test Failures

This session has pre-existing dirty files from prior sessions. The failing tests are in:
- `tests/Unit/Notifications/TrialNudgeNotificationTest.php` — PHP test failure
- `tests/Unit/Notifications/WelcomeSequenceNotificationTest.php` — PHP test failure
- `resources/js/Pages/Onboarding.test.tsx` — 6 JavaScript test failures

These failures require code fixes to:
1. Update notification subject expectations in PHP tests
2. Update Onboarding component test selectors to match new markup structure
