# Billing Review — Sitting 2 (PlanTier enum)

**Session:** 2597c859-8128-4dc6-b536-575f0ea606f9
**Sitting:** sitting-2-plan-tier-enum
**Risk:** Type-only refactor — no subscription mutation logic changed.

## Checklist

- [x] **Test coverage:** 1471 assertions across PlanTier/Billing/PlanLimit filters; 7890 total suite. All pass.
- [x] **Webhook handling:** No webhook dispatch paths changed. `resolveTierFromPrice()` return type changed from `?string` → `?PlanTier`; all callers updated. Wire format unchanged (`.value` serialized before Inertia/JSON).
- [x] **Eager loading:** No Cashier mutation calls added. `BillingService` methods unchanged structurally; eager-load patterns at lines 68-70 untouched.
- [x] **PHPStan:** Level 8, 0 errors. `nullsafe.neverNull` resolved via `PlanTier::safeValue()`.
- [x] **Magic string elimination:** PI-2 grep confirms 0 bare tier literals in service files. Arch test `tests/Arch/PlanTierStringLiteralTest.php` guards against regression.
- [x] **Wire contract:** All Inertia props serialized with `->value` — byte-identical to prior string values. No frontend type drift (npm build ✓, npm test 1608/1608 ✓).
